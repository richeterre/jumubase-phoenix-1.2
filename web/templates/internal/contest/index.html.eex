<h2><%= gettext("Contests") %></h2>

<div class="table-responsive">
  <table class="table">
    <thead>
      <th><%= gettext("Name") %></th>
      <th><%= gettext("Duration") %></th>
      <th><%= gettext("Signup deadline") %></th>
    </thead>
    <tbody>
      <%= for contest <- @contests do %>
        <tr>
          <td><%= link name_with_flag(contest), to: internal_contest_path(@conn, :show, contest) %></td>
          <td><%= "#{contest.start_date}–#{contest.end_date}" %></td>
          <td><%= contest.signup_deadline %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= if authorized?(@current_user, :new, Jumubase.Contest), do:
  link gettext("Add Contest"), to: internal_contest_path(@conn, :new),
  class: "btn btn-primary" %>
